# TODO

- [ ] Fix Flask `/analyze` route indentation + control flow causing `Expected indented block` and preventing DB insert.
- [ ] Run `python -m py_compile app.py` (or `python app.py`) to verify no syntax errors.
- [ ] Quick manual test: login → analyze complaint → confirm analyze result page renders.
- [ ] Verify “Download PDF Report” works from `templates/analyze.html`.

